<h1><%= @forum.name  %></h1>
<p id="notice"><%= notice %></p>
<table>
	<tr>
		<th width="70%">Thread Title</th>
		<th width="10%">Replies</th>
		<th width="10%">Last Post</th>
	</tr>

	<%= will_paginate @topics %>
	<%= render @topics %>
	<%= will_paginate @topics %>

</table>

	<p class="new_thread"><% if signed_in? %><%= link_to "New Thread", "/topics/new?forum=#{@forum.id}" %><% end %></p>
